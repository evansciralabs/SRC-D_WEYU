🌌 CYBER-CONSPECTUS MATRIX
DASHBOARD OPERATIONS MANUAL v1.0
Protocol: Project Genesis / Secure Node Mode

⚙️ INITIALIZATION
> Load: Clone or open the dashboard from the GitHub link.
> Prepare: This interface is your command deck — projects, incidents, and scratch data all run through here.
> Security: Operate locally; do not sync until stable versions are verified.
> 

🧩 CLEAN EXPORT PROTOCOL
 * Create a Clean Template
   * Start fresh, no data loaded.
   * Export this empty state immediately.
   * Label it something like DASHBASE_CLEAN.srcd.
   * This file becomes your zero-state import — your reset button.
 * Important:
   * The Scratchpad saves its own data.
   * Clear the Scratchpad before exporting your clean file, or ghosts of old data will remain.

🧱 PROJECT CREATION SEQUENCE
> Projects and incidents are modular nodes.
> Each export = one clean data corridor.
> Keep separate exports to maintain system clarity.
> 

Steps:
 * Create Project / Incident
 * Fill out:
   * Title
   * Due Date
   * Status
   * Summary / Notes
 * Input deeper development logs in the Scratchpad.
 * Save Scratchpad File:
   * Default name: timestamped .srcd.json
   * Rename → .srcd (remove .json)
   * This allows proper linking back into project entries.

🧠 SCRATCHPAD I/O
> Think of the Scratchpad as your AI co-processor.
> 

When saving:
 * Keep timestamp naming for traceability.
 * Copy timestamp → paste it into tooltip info for quick reference.
 * Tooltip = short descriptor, e.g. "Scene Planning", "Source Draft", "Render Notes".
 * .srcd files can store structured text, prompts, metadata, or short scripts.

🛰️ LINKING FILES
 * Go to your Project node.
 * Add the .srcd file (and label it).
 * Files like images or PDFs can also attach — they stay on-device for safety.
 * This keeps your dashboard clean, modular, and AI-readable.

🧩 AI SYNC LOGIC
> Each exported dashboard is self-contained and context-rich.
> 
 * If you export one project per dashboard, AI models read .srcd with full clarity.
 * When limits hit, reuse output → import into another clean instance.
 * Merge manually when necessary for precision control.
 * Master dashboards stay pure; project exports stay lean.
🧠 TL;DR / QUICKFLOW
 * Load dashboard
 * Create project / incident
 * Input notes + scratchpad
 * Save scratchpad → rename to .srcd
 * Link .srcd to project with tooltip
 * Export clean dash template for reset
 * Export project dashes individually
🧩 SECURITY REMINDER
 * Work offline when possible.
 * Keep clean exports versioned and labeled.
 * Treat .srcd files as encrypted logic fragments — your intellectual DNA.
✦ End of Transmission ✦


System codename: SRC-D_WEYU :: Project Sync Protocol
Operator: MΛVΞRΙCK
